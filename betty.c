#include "shell.h"

/**
 * pass_betty - a function that passes Betty check
 *
 * Return: Always 0 (on success).
 */
int pass_betty(void)
{
	shell_print("Hello World! We passed Betty Checks\n");
	return (0);
}
